(This folder was previously used to store specialty images, but the app now displays icons only. You can safely delete the contents or ignore this folder.)

If you later decide to re-enable pictures, add files here with matching names and reintroduce `imageUrl` fields in `app/lib/provinces.ts`.